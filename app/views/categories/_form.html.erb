<%= form_for @category do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <div class="cancel-submit">
    <%= button_to "Cancel", categories_path, class: 'btn btn-outline-light' %>
    <%= f.submit class: 'btn btn-outline-light'%>
  </div>

<% end %>
