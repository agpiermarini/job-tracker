<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <div class="cancel-submit">
    <%= button_to "Cancel", categories_path, class: 'btn btn-outline-warning' %>
    <%= f.submit class: 'btn btn-outline-success' %>
  </div>

<% end %>
